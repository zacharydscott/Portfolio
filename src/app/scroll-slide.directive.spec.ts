import { ScrollSlideDirective } from './scroll-slide.directive';

describe('ScrollSlideDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollSlideDirective();
    expect(directive).toBeTruthy();
  });
});
